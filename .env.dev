# Development runtime overrides
API_HOST=0.0.0.0
API_PORT=8000
FRONTEND_ORIGIN=http://localhost:5173

# Dev database (recommend local MySQL)
# If you want to use a remote MySQL, make sure the MySQL user is granted for your client IP,
# otherwise you may see: "Host 'x.x.x.x' is not allowed to connect to this MySQL server".

